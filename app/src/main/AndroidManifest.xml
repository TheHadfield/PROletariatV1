<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proletariatv1">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PROletariatV1">
        <activity android:name=".GuestActivity2"></activity>
        <activity android:name=".ManageActivity" />
        <activity android:name=".ComposeActivity2" />
        <activity android:name=".ComposeActivity" />
        <activity android:name=".LoggedInActivity" />
        <activity android:name=".CreateActivity2" />
        <activity android:name=".SignActivity" />
        <activity android:name=".GuestActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity
            android:name=".ui.login.LIActivity"
            android:label="@string/title_activity_liactivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>